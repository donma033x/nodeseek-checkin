[project]
name = "nodeseek-checkin"
version = "1.0.0"
description = "NodeSeek 论坛自动签到"
requires-python = ">=3.10"
dependencies = [
    "curl_cffi>=0.5.0",
    "playwright>=1.40.0",
    "requests>=2.28.0",
]
